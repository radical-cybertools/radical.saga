
# this config file will run the job package unit tests against over a local ssh
# connection

{
    "job_service_url"    : "ssh://boskop/",

    "context_type"       : "ssh",
  },

  "benchmarks" :
  {
    "concurrency"        : 20,
    "iterations"         : 800,
    "load"               : 1,
    "name"               : "ssh://boskop"
  }
}

