
{ 
    # colon separated list of python module pathes to load adaptors from
    "adaptor_path"       : "${RADICAL_SAGA_ADAPTOR_PATH}",

    # load adaptors which are marked as beta (i.e. not released)
    "load_beta_adaptors" : "${RADICAL_SAGA_ENABLE_BETA:False}",
}

